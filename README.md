# UW Compass

An AI-powered resource finder for University of Washington students. Describe your situation in plain language and get pointed to the right official UW resources, with explanations and a clear next step.

> CSS 382 — Introduction to Artificial Intelligence · DYOP group project

## Quick start

```bash
npm install
cp .env.example .env.local        # then put your OpenAI key in .env.local
npm run seed                      # generates embeddings for the curated resource set
npm run dev                       # http://localhost:3000
```

You need an [OpenAI API key](https://platform.openai.com/api-keys) **with billing enabled**. Seeding all 30+ resources costs a fraction of a cent on `text-embedding-3-small`.

## What it does

1. Student types something like *"I'm overwhelmed, behind in math, and need a quiet place to study."*
2. The app extracts **structured needs** (`academic`, `study_space`) with intensities and supporting evidence using GPT-4o-mini with function calling.
3. In parallel, it embeds the same input with `text-embedding-3-small`.
4. It computes a combined score against every resource in the database — embedding similarity, category match, tag overlap, and urgency boost — then takes the top 5 with category diversification.
5. GPT-4o-mini writes a short per-resource explanation and a 2–4 step action plan.
6. The UI shows the extracted needs, ranked recommendations with links to official UW pages, and the next-step plan.

## Architecture

```
Browser
  │  POST /api/recommend  { input }
  ▼
app/api/recommend/route.ts
  │
  ├── lib/ai.ts          → OpenAI text-embedding-3-small (input embedding)
  ├── lib/ai.ts          → OpenAI gpt-4o-mini + function call (need extraction)
  ├── lib/db.ts          → better-sqlite3 → data/compass.db (resources + precomputed embeddings)
  ├── lib/recommend.ts   → cosine similarity + tag/category/urgency scoring + diversification
  └── lib/ai.ts          → OpenAI gpt-4o-mini + function call (per-resource why + next steps)
  │
  ▼
{ needs, recommendations, next_steps }
```

Three OpenAI calls per request: 1 embedding + 2 chat completions (the embedding and need extraction run in parallel). Resource embeddings are computed once at seed time, not per request.

## Project layout

```
app/
  layout.tsx              root layout
  page.tsx                client-side form + results UI
  globals.css             tailwind entrypoint
  api/recommend/route.ts  POST endpoint
lib/
  types.ts                Resource, ExtractedNeed, Recommendation, Category
  db.ts                   SQLite read/write (better-sqlite3)
  ai.ts                   OpenAI calls: embed, extractNeeds, summarize
  recommend.ts            cosine similarity + ranking + diversification
data/
  resources.json          curated UW resources (edit this to add/update)
  compass.db              generated by `npm run seed` (gitignored)
scripts/
  seed.ts                 reads resources.json, computes embeddings, writes to SQLite
```

## Updating the resource set

Resources live in [`data/resources.json`](data/resources.json). Each entry has:

```ts
{
  id: string;            // stable slug, used as DB primary key
  name: string;          // display name
  category: 'academic' | 'wellness' | 'basic_needs' | 'transportation'
          | 'study_space' | 'career' | 'financial';
  campus: 'seattle' | 'bothell' | 'tacoma' | 'all';
  description: string;   // 1–3 sentences; this is what gets embedded
  url: string;           // official UW page
  tags: string[];        // snake_case fine-grained tags
  urgent: boolean;       // true for crisis-time resources (SafeCampus, Husky HelpLine, etc.)
}
```

After editing the JSON, re-run `npm run seed` to refresh embeddings.

**Important:** Verify URLs before demo. UW reorganizes their sites occasionally. The risk table in the proposal calls this out — there's no automated link-check yet.

## Tuning the ranker

Weights live in [`lib/recommend.ts`](lib/recommend.ts):

```ts
weights: {
  embedding:     0.5,   // semantic similarity (cosine of student input vs. resource description)
  categoryMatch: 0.25,  // does the resource's category match an extracted need?
  tagOverlap:    0.15,  // share of extracted tags that appear in the resource's tag list
  urgencyBoost:  0.1,   // student appears urgent AND resource is flagged urgent
}
```

Adjust and re-test. Top-K and per-category cap (default: max 2 of any one category in the top 5) are also there.

## Scenario testing

The proposal's evaluation scenarios:

- "I am failing calculus and need help."
- "I am stressed and cannot focus."
- "I commute and need transportation help."
- "I need food support and financial help."
- "I need a quiet place to study and career guidance."

Test these from the UI or with `curl`:

```bash
curl -s -X POST http://localhost:3000/api/recommend \
  -H "content-type: application/json" \
  -d '{"input":"I am failing calculus and need help."}' | jq
```

## Deployment

The Vercel-style deployment path needs one change: SQLite's local file doesn't survive in a serverless environment. Options:

1. **Vercel + Turso** (recommended for stretch): swap `better-sqlite3` for `@libsql/client` against a Turso database.
2. **Render or Railway full Node host**: keeps `better-sqlite3` and the `data/compass.db` file. Add the seed step to the deploy script.

The frontend itself deploys cleanly to Vercel either way; only the data layer needs the swap.

## What's done vs. what's next

Done:
- [x] Curated resource database (30+ entries across all 7 categories)
- [x] SQLite schema + seed pipeline with precomputed embeddings
- [x] OpenAI embedding + need-extraction + summarization
- [x] Ranked recommendations with multi-signal scoring + category diversification
- [x] Single-page React UI with example prompts, results display, urgent-banner

Next (stretch goals from proposal):
- [ ] Campus filter (Seattle / Bothell / Tacoma)
- [ ] Feedback buttons ("helpful" / "not helpful") with anonymous logging
- [ ] Saved recommendations (localStorage)
- [ ] Multilingual input
- [ ] Link-health check script

## License

Educational project. Resource content links to official UW pages and is not a substitute for them. For emergencies call 911 or contact [SafeCampus](https://www.washington.edu/safecampus/).
